apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: ueransim-flux
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: kpt package for deploying free5gc ueransim helm charts via flux
